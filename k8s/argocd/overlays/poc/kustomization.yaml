apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

bases:
  - ../../base

resources:
  - secrets.poc.yaml
  - applications.yaml
  - clusters.poc.yaml
  - projects.yaml

patchesStrategicMerge:
  - argocd-cm.yaml
